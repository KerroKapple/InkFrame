// Studio 标签体。StudioTopChrome 已随外壳 chrome 收敛删除，这里只剩内容。
import 'package:flutter/material.dart';

import '../../../studio/studio_home_screen.dart';

class StudioTab extends StatelessWidget {
  const StudioTab({super.key});

  @override
  Widget build(BuildContext context) => const StudioHomeScreen();
}

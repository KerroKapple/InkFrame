# ON-4 网络错误文案走查 + i18n pass Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 上线前一次性全量文案走读（plan §5）：① ON-4 核心=错误文案区分「网络不可达 vs key 无效」且带下一步动作；② 中英语气/带参键语序/标点风格一致；③ 标记僵尸键。产出=一批 ARB 修订 + 僵尸键处置，单 PR。

**Architecture:** 三轴并行审读（错误文案轴 / 全量一致性轴 / 僵尸键 grep 轴）→ 主会话汇总裁决 → ARB 编辑 + gen-l10n → 闸门 → PR。

## Global Constraints

- 不做：provider displayName 翻译、快捷键符号进 ARB、第三语言（plan §5 明确排除）。
- 僵尸键判定须防间接引用假阳性（l10n_x.dart 的 messageKey 映射、动态 getter）。
- 改文案不改键名（键名变更会波及测试 finder，无必要不动）。
- en/zh 键集奇偶由 arb_hygiene 测试闸住；gen-l10n 产物同 commit。

### Task 1: 三轴并行审读（agents）
- [ ] 错误轴：errorNetwork*/errorInvalidKey/settingsApiKeySavedUnverified 等错误链路键——网络 vs key 无效可区分性、下一步动作、zh 措辞。
- [ ] 一致性轴：全量 464 行 en/zh 对读——语气（zh 不用敬语堆砌/en 不用被动堆砌）、带参键语序、标点（zh 全角/en 半角）、截断风险。
- [ ] 僵尸键轴：每键 grep lib/ 使用点（`.键名` 形式 + l10n_x 间接映射），产出无引用键清单+置信度。

### Task 2: 汇总落改
- [ ] 裁决三轴产出（改/不改/记债），编辑 app_en.arb + app_zh.arb，gen-l10n。
- [ ] 僵尸键：确认死键删除（en+zh+generated 同步）；存疑保留并记 BOARD。
- [ ] 闸门：analyze 0 + 全套绿（文案改动可能碰测试 finder——同步修）。

### Task 3: docs 登记 + PR
- [ ] BOARD/MASTERPLAN 登记（同 PR）；PR → 对抗评审 → CI → squash merge。

# S4 — Generation + Studio + Settings features

**CONCLUSION: 发现 5 条**（密钥安全专项干净）

## Findings

### S4-1 | P2 | [Styles] | `lib/features/studio/studio_home_screen.dart:95` | `[已确证]`
`typo.display.copyWith(fontSize: 32, color: colors.fg1)` 内联覆盖字号，违反 [Styles]。`check-inline-styles.sh` 命中 [HARDCODED_FONT_SIZE]。
- **修法**：typography 增 `displayLg` 之类 token 替换 display，删 `fontSize:32`。

### S4-2 | P3 | [i18n] | `lib/features/studio/widgets/studio_top_chrome.dart:59-61` | `[已确证]`
`Text('Ink')/'/'/'Frame'` 拆分品牌词。品牌名可豁免，但拆三段对朗读/RTL 不友好。
- **修法**：可接受现状；或给 logo Row 包 `Semantics(label:'InkFrame')`。

### S4-3 | P3 | [i18n/correctness] | `lib/features/studio/widgets/library_sidebar.dart:62` | `[已确证]`
ARCHIVE section `trailing: '0'` 写死归档计数(功能 stub)，接真实计数时易漏改。
- **修法**：接入数据源后用真实 count，或加 TODO 标注。

### S4-4 | P2 | [Lifecycle] | `lib/features/generation/generation_controller.dart:256` | `[需复核]`
`unawaited(_track(...))` 启动的后台 future 持有 `handle.status.listen` 订阅，仅在 `_track` 自身 finally(:359)取消。controller 由 `FutureProvider`(:47，非 autoDispose 未显式 keepAlive)创建；controller 被释放时无法主动中止 in-flight `_track`。注释(:56-57)称后台 _track 全程不再触 ref，故无 use-after-dispose，风险仅是无法主动中止。
- **修法**：可接受；若要严谨，持有 in-flight 集合在 `ref.onDispose` 取消，或显式 keepAlive。

### S4-5 | P2 | [Errors] | `lib/features/studio/studio_home_screen.dart:443` | `[已确证]`
`_ProjectGrid` 打开画布 `onTap` 用裸 `catch (_)` 吞所有异常仅弹通用 toast，丢失 InkError 类型(对比同文件 :159 正确用 `on InkError`)。`openProjectCanvas`→`createCanvas` 可能抛 InkError。
- **修法**：改 `on InkError catch (e)` 用 `l10nError(context, e)` 按 code 映射文案。

## COVERAGE
Read 全部 S4 scope(generation/studio/settings 各文件，跳过 *.freezed.dart)。跑 check-inline-styles/magic-strings/disposable-cleanup(Windows 下部分以手工 Read 复核命中)。
**密钥安全专项(最关键)结论**：API key 全程只经 `SecureStorageService`(generation_controller:154、api_key_scope_controller:40/60)；从不进 logger(extra 仅 job_id/provider_id/node_id)；不进 Clipboard(storage_path_section 复制 DB 路径非 key)；保存成功即 `_ctrl.clear()`——**未发现 P0/P1 密钥泄露或正确性问题**。check-inline-styles 对 `InkSpacing.s10`/`InkRadius.*` 的命中为正则误报(token 含数字)，已 Read 确认非硬编码。

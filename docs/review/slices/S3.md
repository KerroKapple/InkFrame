# S3 — Canvas feature

**CONCLUSION: 发现 7 条**

## Findings

### S3-1 | P1→复核降 P2 | [i18n] | `lib/features/canvas/widgets/canvas_inspector.dart:65-77,97,113`(及 :54) | `[已确证]`
渲染屏(CanvasScreen → CanvasInspector)上直出硬编码英文用户串，违反铁律2。`_KvRow('Position','140,220,0')`/`_KvRow('Focal length','50 mm')`(65-77)、:97 `'Establishing the docks. Hold for 4 s…'`、:113 `'+ ${l.canvasInspectorAddAttribute}'`("+ " 前缀也是裸串)。magic-strings checker 漏报(单引号且非 `Text("...")` 构造器)。
**复核纠正**：65-77 是未接线的 **mock 占位数据**(假相机参数)，根因是该 inspector 是静态 mockup；`'+ '` 前缀是接线后仍存在的真实 i18n 泄漏。故降 P2。
- **修法**：走 context.l10n(新增 ARB key)；mock 占位数据接真实 node 属性或移出 live 屏；"+ " 并入 ARB 文案。

### S3-2 | P2 | [i18n] | `canvas_inspector.dart:54` | `[需复核]`
`nodeKindLabel.toUpperCase()`——源串已 l10n，但 `.toUpperCase()` 对 tr/de(i/İ)等 locale 不安全。
- **修法**：大写形态在 ARB 预置或用 locale-aware 转换。

### S3-3 | P2→复核降 P3 | [Lifecycle] | `lib/features/canvas/providers/canvas_edges_controller.dart:65,68,79,98` | `[已确证]`
`AutoDisposeFamilyAsyncNotifier`，`addEdge/removeEdge/updateRole` 在 `await _repo...` 后无条件 `state=...`，无 `_alive`(对比同目录 `canvas_nodes_controller.dart:34-39` 的 ME-27 守卫)也无 `keepAlive`。窄窗竞态(await 期间 provider 被 autoDispose)下写 disposed notifier 抛 StateError。
**复核确认**且指出 finding 低估了：成功路径(65)与回滚路径(68/79/98)**都**未防护。
- **修法**：复制 ME-27 的 `_alive` 模式，或乐观写路径用 `ref.keepAlive()`。

### S3-4 | P2→复核降 P3 | [Lifecycle] | `lib/features/canvas/providers/link_action_controller.dart:48,50` | `[已确证]`
同 S3-3，`AutoDisposeFamilyNotifier.linkTo` 双重暴露：内层 addEdge 抛的 StateError 不是 InkError，会穿过 :49 的 `on InkError` 冒泡；自身 :48 写 state 也无保护。

### S3-5 | P2 | [Styles] | `canvas_inspector.dart:95,190` | `[已确证]`
裸 `EdgeInsets.only(top:2)`/`symmetric(vertical:3)`，InkSpacing 无 2/3 档(最小 xs=4)。checker 命中。

### S3-6 | P3 | [Styles] | `canvas_left_toolbar.dart:73` / `canvas_node_card.dart:99` | `[已确证]`
裸数字 EdgeInsets(`margin: symmetric(vertical:2)`；fromLTRB 含裸 `2`)，均为"仅视觉/mockup-parity"组件。

### S3-7 | P3 | [Styles] | `video_lightbox.dart:163` | `[已确证]`
`Text(..., style: TextStyle(color: colors.fg1))` 用裸 `TextStyle` 而非 `inkTypography.*`(颜色已 token)。
- **修法**：`style: typo.caption.copyWith(color: colors.fg1)`。

## COVERAGE
全文件 Read：models/(canvas_edge/canvas_node 含 fromRow 容错)、util/(edge_hit_test 点线距离数学正确、node_position、canvas_job_effects diff 去重正确)、全部 providers、widgets 全 18 个(canvas_view 527、node_card 379、video_lightbox 220 等)。`check-inline-styles.sh` 全 widgets(误报 s10/s18 token 已剔除)；`check-disposable-cleanup.sh`(video_lightbox 等 exit 0，dispose 正确)。`check-magic-strings.sh` 在 Git Bash 上 `grep -P` unicode 病态超时(exit 124)，i18n 改人工 Read 得出(发现 checker 盲区)。
**干净项**：几何数学正确；canvas_job_listener+effects 无重复 job/竞态;CanvasNodesController `_alive` 守卫正确;media_kit 资源释放正确;拖拽状态一致;无空指针/漏 await/off-by-one。

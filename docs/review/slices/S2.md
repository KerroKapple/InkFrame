# S2 — Theme & design-token system

**CONCLUSION: 发现 7 条**

## Findings

### S2-1 | P2 | [Styles] | `lib/theme/primitives/ink_accent_chip.dart:28` | `[已确证]`
硬编码 `EdgeInsets.symmetric(horizontal: 10, vertical: 4)`，而 tokens 里恰好有精确档位(`InkSpacing.s10`=10、`InkSpacing.xs`=4)。`check-inline-styles.sh` 报 [HARDCODED_SPACING]。注释 `padding 10x4` 进一步证明这是有意固定值。
- **修法**：`EdgeInsets.symmetric(horizontal: InkSpacing.s10, vertical: InkSpacing.xs)`。

### S2-2 | P3 | [Styles] | `ink_accent_chip.dart:35` | `[已确证]`
边框 `width: 0.8` 裸字面值，无 stroke-width token。
- **修法**：引入 `InkBorder.thin` 之类 token 统一描边宽度。

### S2-3 | P2 | [Styles] | `ink_dashed_slot.dart:17` | `[已确证]`
默认 padding 硬编码 `horizontal: 10, vertical: 6`(10 有 `InkSpacing.s10`)。checker 命中。
- **修法**：水平改 `InkSpacing.s10`；垂直 6 无档则补 token。

### S2-4 | P2 | [Styles] | `ink_surface_button.dart:69` | `[已确证]`
常规 flavor padding 硬编码 `horizontal: 10, vertical: 6`。与 ink_dashed_slot 同一裸值，说明缺统一引用。

### S2-5 | P3 | [Styles] | `ink_accent_chip.dart:41`(及 ink_surface_button:64/74、ink_ghost_button:60、ink_amber_button:45、ink_window_chrome:127) | `[已确证]`
图标尺寸 `size: 16/18/14` 各写各的，无 icon-size token。
- **修法**：tokens.dart 增设 `InkIconSize`(sm/md/lg)统一引用。

### S2-6 | P3 | [Styles] | `lib/theme/components/ink_window_chrome.dart:26`(及 123-124、127) | `[已确证]`
窗口 chrome 几何裸值(`height:56`、按钮 `40×32`、`size:14`)。components 目录被 checker 豁免，需人工核；属组件固有几何但未像 `InkProgressBar._kHeight` 那样具名。
- **修法**：提取带注释的 `static const` 几何常量。

### S2-7 | P2 | [Tests] | `lib/theme/tokens.dart:14` | `[已确证]`
注释声称 `InkPalette.surfaceCanvasDark == InkColors.dark().surfaceCanvas` 由 `tokens_test` 守护，但 `test/theme/tokens_test.dart` 全文无此断言(已通读 252 行)。当前两值都为 `0xFF0B0908`(:20 与 :57)故非现行 bug，但文档承诺的不变量未被测试覆盖，将来易静默漂移。
- **修法**：tokens_test 增 `expect(InkPalette.surfaceCanvasDark, InkColors.dark().surfaceCanvas)`，或修正注释。

## COVERAGE
逐行通读全部 in-scope 文件：tokens.dart(241)、typography.dart(176)、motion.dart(48)、app_theme.dart(96)、primitives/* 全 7 个、components/* 全 6 个；对 7 个 primitives 跑 `check-inline-styles.sh`，components 目录(脚本豁免)手动逐行核裸值；交叉读 tokens_test.dart 核实守护缺失。
**干净项**：三主题语义槽位齐全、`buildAppTheme` 映射完整、`ink_compact_text_field` FocusNode 正确释放、`ink_window_chrome` 走 l10n、无密钥/裸 catch/漏 await、const 正确。
